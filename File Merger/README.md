# Excel File Merger

A simple Streamlit app that lets users upload multiple Excel (`.xlsx`) files, validate their headers, merge the data, detect duplicate rows, and download the merged result as CSV.

## How to Run

```bash
streamlit run app.py
```
